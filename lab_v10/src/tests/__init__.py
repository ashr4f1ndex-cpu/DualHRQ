"""Test suite package for the dual‑book trading lab.

This file marks the tests directory as a package, enabling
relative imports of modules under `src` when running the tests
with tools like pytest.  Without this file Python may treat
`src/tests` as a normal directory, making imports fail.
"""