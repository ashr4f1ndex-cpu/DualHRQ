"""Common utilities for the dual‑book trading lab.

Currently contains performance metrics (Sharpe, Sortino, DSR,
maximum drawdown) used throughout both the options and intraday
components.  This initializer makes the `common` directory a
package so its modules can be imported via relative paths.
"""